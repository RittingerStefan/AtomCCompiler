package lexer;

public enum State {
    STATE_START,
    STATE_DIV,
    STATE_MINUS,
    STATE_COMMENT,
    STATE_NOT,
    STATE_ASSIGN,
    STATE_LESS,
    STATE_GREATER,
    STATE_AND_AUX,
    STATE_OR_AUX,
    STATE_CHAR_START,
    STATE_CHAR_ESCAPE,
    STATE_CHAR_VAL,
    STATE_STR_START,
    STATE_STR_VAL,
    STATE_STR_ESCAPE,
    STATE_IDENTIFIER,
    STATE_NUM_DEC,
    STATE_NUM_AUX,
    STATE_NUM_OCT,
    STATE_NUM_HEX,
    STATE_DOT_AUX,
    STATE_REAL,
    STATE_EXP,
    STATE_EXP_SIGN,
    STATE_REAL_WITH_EXP
}
