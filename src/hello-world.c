struct test {
     int x;
     bool y[10];
};

struct test2 {
    test abc;
};

int sum(int x, int y) {
    return x + y;
}

int main() {
    test2 a;
    bool c;
    int b;

    int w;
    w = (20 + 4) / 8 + 20*b;
    return 0;
}